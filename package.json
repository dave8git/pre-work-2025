{
    "scripts": {
        "start": "npm-run-all --parallel live-server --port=3000 scss",
        "live-server": "live-server --port=3000",
        "scss": "sass --watch src/scss:src/css"
    },
    "devDependencies": {
        "npm-run-all": "^4.1.5",
        "sass": "^1.85.1"
    }
}